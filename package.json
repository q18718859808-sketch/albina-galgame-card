{
  "name": "albina-galgame-card-source",
  "version": "1.0.44",
  "private": true,
  "type": "module",
  "scripts": {
    "assets:generate": "node scripts/audit-assets.mjs --write",
    "assets:audit": "node scripts/audit-assets.mjs",
    "build": "vite build",
    "typecheck": "vue-tsc --noEmit",
    "test": "vitest run",
    "release:sync": "npm run build && npm run assets:generate && node scripts/build-release.mjs && npm run assets:audit"
  },
  "dependencies": {
    "pinia": "^3.0.3",
    "vue": "^3.5.17",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^24.0.10",
    "@vitejs/plugin-vue": "^6.0.0",
    "typescript": "^5.8.3",
    "vite": "^7.0.4",
    "vitest": "^3.2.4",
    "vue-tsc": "^3.0.1"
  }
}
